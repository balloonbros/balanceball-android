package cc.balloonbros.balanceball.lib;

public class ResourceBase {
    public String _s(int stringId) {
        return _.s(stringId);
    }

    public int _i(int integerId) {
        return _.i(integerId);
    }

    public int _c(int colorId) {
        return _.c(colorId);
    }
}
